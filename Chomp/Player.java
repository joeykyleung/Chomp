public class Player
{

/*
	public int move(Chip[] chip)
	{
		if(chip[8].isAlive==false && chip[19].isAlive==true)
		{
			return(19);
		}

		if(chip[19].isAlive==false && chip[8].isAlive==true)
		{
			return(8);
		}

		return(9);
	}
*/
}

